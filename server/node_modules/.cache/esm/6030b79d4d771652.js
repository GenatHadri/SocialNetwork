let register;_4d7‍.w("../controllers/auth",[["register",["register"],function(v){register=v}]]);const express = require("express");

const router = express.Router();

//controllers

router.post("/register", register);

module.exports = router;
